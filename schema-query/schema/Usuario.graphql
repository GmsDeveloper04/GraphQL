type Perfil {
  id: Int
  nome: String
}

enum UsuarioStatus {
  ATIVO,
  INATIVO,
  BLOQUEADO
}

type Usuario {
  id: ID
  nome: String!
  email: String!
  idade: Int
  salario: Float
  vip: Boolean
  chiclete : String
  perfil: Perfil,
  status: UsuarioStatus
}
